version: '3'
services:
  db:
    image: mongo
    container_name: db
    volumes:
      - mongo_data:/data/db
    networks:
      - backend

  express:
    build:
      context: ./main
      dockerfile: Dockerfile
    container_name: express-server
    ports:
      - "4444:4444"

    depends_on:
      - db
    networks:
      - backend

volumes:
  mongo_data:

networks:
  backend:
    driver: bridge